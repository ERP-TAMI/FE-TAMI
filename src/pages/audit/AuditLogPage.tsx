import { PlaceholderPage } from "@/components/shared/PlaceholderPage";

export default function AuditLogPage() {
  return (
    <PlaceholderPage
      module="Audit"
      title="Audit log"
      description="Audit history workflows will be defined and implemented in a dedicated feature task."
    />
  );
}
